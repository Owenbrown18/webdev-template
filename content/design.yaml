colors:
  brand:
    primary: "#103529"
    secondary: "#0f172a"
    accent: "#f59e0b"
  text:
    primary: "#0f172a"
    secondary: "#1f2937"
    muted: "#475569"
    inverse: "#f8fafc"
    inverseMuted: "#e2e8f0"
  surface:
    base: "#f8fafc"
    card: "#ffffff"
    muted: "#f1f5f9"
  border:
    subtle: "rgba(15, 23, 42, 0.08)"
    strong: "rgba(15, 23, 42, 0.18)"
  navigation:
    hoverBg: "rgba(16, 53, 41, 0.08)"
    activeBg: "#103529"
    activeText: "#f8fafc"
    activeShadow: "0 18px 36px -22px rgba(16, 53, 41, 0.35)"
typography:
  fontFamily:
    heading: "'Satoshi', 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif"
    body: "'Satoshi', 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif"
  scale:
    display:
      size:
        mobile: "clamp(2.5rem, 4vw, 3.75rem)"
        tablet: "clamp(2.5rem, 4vw, 3.75rem)"
        desktop: "clamp(2.5rem, 4vw, 3.75rem)"
      lineHeight: "1.08"
      weight: 600
    heading:
      size:
        mobile: "clamp(2rem, 3vw, 2.85rem)"
        tablet: "clamp(2rem, 3vw, 2.85rem)"
        desktop: "clamp(2rem, 3vw, 2.85rem)"
      lineHeight: "1.15"
      weight: 600
    subheading:
      size:
        mobile: "clamp(1.25rem, 2.2vw, 1.55rem)"
        tablet: "clamp(1.25rem, 2.2vw, 1.55rem)"
        desktop: "clamp(1.25rem, 2.2vw, 1.55rem)"
      lineHeight: "1.6"
      weight: 500
    body:
      size:
        mobile: "clamp(1rem, 1.6vw, 1.2rem)"
        tablet: "clamp(1rem, 1.6vw, 1.2rem)"
        desktop: "clamp(1rem, 1.6vw, 1.2rem)"
      lineHeight: "1.9"
      weight: 400
    eyebrow:
      size:
        mobile: "clamp(2.45rem, 5.5vw, 5.25rem)"
        tablet: "clamp(2.45rem, 5.5vw, 5.25rem)"
        desktop: "clamp(2.45rem, 5.5vw, 5.25rem)"
      lineHeight: "1.2"
      weight: 600
      letterSpacing: "0.35em"
      uppercase: true
    finePrint:
      size:
        mobile: "clamp(0.85rem, 1.2vw, 0.95rem)"
        tablet: "clamp(0.85rem, 1.2vw, 0.95rem)"
        desktop: "clamp(0.85rem, 1.2vw, 0.95rem)"
      lineHeight: "1.55"
      weight: 500
      letterSpacing: "0.06em"
